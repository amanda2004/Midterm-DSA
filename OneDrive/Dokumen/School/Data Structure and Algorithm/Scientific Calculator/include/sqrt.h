#ifndef SQRT_H
#define SQRT_H
#include "History.h" //Include "History.h" header file for dependencies

//Defines 'sqrtCalculator' class
class sqrtCalculator
{
public:
    //Declaration of a public member function named 'start'
    //It takes one argument that reference to the object type 'history_list'
    void start (history_list& history);
};

#endif // SQRT_H

#ifndef NATURALLOGARITHM_H
#define NATURALLOGARITHM_H
#include "History.h" //Include the "History.h" header file for dependencies

//This is the declaration of 'naturallogarithm' class
class naturallogarithm
{
    public:
        //This is the declaration of a public member function named 'start'
        //It takes one argument, which is a reference to an object of type 'history_list'
        void start (history_list& history);
};

#endif // NATURALLOGARITHM_H
